# FAKEDATA_INDEX — 26C Network Analytics Dashboard

| ID | Description | Columns | Used In | Real Replacement | Stage 2 Status | Stage 3 Status |
|----|-------------|---------|---------|-----------------|----------------|----------------|
| FAKE_001 | Bipartite graph summary: node/edge counts, geography & committee type | type, geography, committee_type, count | `output$bipartiteStats`, `updateGeoChart`, `updateTypeChart` | `g` via `read_rds("data/committees/graph_bipartite.rds")` | ⏳ Still fake (`get_fake_bipartite_summary()`) | — |
| FAKE_002 | Coaffiliation edges (weight > 1) and isolate degree distribution | from, to, weight, geography | `output$sharedMembersTable`, `output$isolateStats`, `updateIsolateDegChart` | `gco` from `coaffiliate(g, type=FALSE, ...)` | ⏳ Still fake (`get_fake_coaff_edges()`, `get_fake_isolate_summary()`) | — |
| FAKE_003 | Node centrality metrics: degree, weighted degree, betweenness, distance | name, geography, deg, wdeg, betw, steps_to_23 | `output$centralityStats`, `output$centralityTable`, `updateDegDensityChart`, `updateDistanceChart` | `gco %>% mutate(deg, wdeg, betw, steps)` reactive on `input$focalNode` | ⏳ Still fake (`get_fake_centrality()`, `get_fake_distance_by_focal()`) | — |
| FAKE_004 | Community detection results (infomap + fast-greedy k=3) | community, count, mean_wdeg, sd_wdeg, dominant_geo | `output$clusterStatsTable`, `updateCommunityBarChart` | `gco %>% mutate(community = group_infomap())` reactive on `input$communityAlgo` | ⏳ Still fake (`get_fake_communities()`) | — |
| FAKE_005 | Geography sub-graph statistics from purrr iterative coaffiliation | geography, n_members, n_committees, local_seats | `output$localCoaffTable`, `updateGeoSubgraphChart` | `gmem` from `map(~coaffiliate()) %>% graph_join_list()` reactive on `input$geoFilter` | ⏳ Still fake (`get_fake_geo_stats()`) | — |
| PLACEHOLDER_001 | Bipartite network visualization (igraph/ggraph) | — | `output$bipartiteNetPlaceholder` → Stage 3: `plotOutput("bipartiteNet")` | `ggraph(g, "fr") + geom_edge_link() + geom_node_point(aes(color=type))` | ⏳ Stub (`renderUI` placeholder div) | — |
| PLACEHOLDER_002 | FR layout ggplot2 visualization | — | `output$layoutVisPlaceholder` → Stage 3: `plotOutput("layoutVis")` | `ggplot() + geom_segment(edges) + geom_point(layout, aes(fill=type))` | ⏳ Stub (`renderUI` placeholder div) | — |
| PLACEHOLDER_003 | Coaffiliation network plot (ggraph) | — | `output$coaffNetPlaceholder` → Stage 3: `plotOutput("coaffNet")` | `ggraph(gco) + geom_edge_link(aes(width=weight)) + geom_node_point(aes(size=deg))` | ⏳ Stub (`renderUI` placeholder div) | — |
| PLACEHOLDER_004 | Community-colored network plot | — | `output$communityNetPlaceholder` → Stage 3: `plotOutput("communityNet")` | `ggraph(gco) + geom_node_point(aes(color=community))` | ⏳ Stub (`renderUI` placeholder div) | — |
